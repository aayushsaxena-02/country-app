import React from "react";

function Searchbox(props){
    return(
         <div>
           <input type="search"  onChange="props." onChange={props.handleChange} />
        </div>
          
    );
};

export default Searchbox;